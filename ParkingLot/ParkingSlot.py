class ParkingSlot:
    def __init__(self, vehicleType, isParked, floor, slot):
        self.vehicleType = vehicleType
        self.isParked = isParked
        self.floor = floor
        self.slot = slot
